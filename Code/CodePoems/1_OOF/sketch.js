function setup() {
  createCanvas(windowWidth, windowHeight);
  background(0, 0, 255);
}

function draw() {
  fill(255, 255, 0);
  textAlign(LEFT);
  textSize(70);
  textStyle(NORMAL);
  text("우프", width / 2, height / 2);
   
}